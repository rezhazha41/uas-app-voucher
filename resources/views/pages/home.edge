<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>voucherku.com</title>

  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body class="bg-gray-100 text-gray-800 font-sans">

<header class="bg-blue-600 text-white p-4 shadow-md flex justify-between items-center">
  <h1 class="text-xl font-bold">Voucherku.com</h1>

  @if (auth.user)
    <div class="flex items-center gap-4">
      <a href="{{ route('voucher_pulsa.history') }}" class="hover:bg-blue-500 p-2 rounded-full" title="Riwayat Belanja">
        <!-- Icon keranjang (gunakan emoji atau SVG sesuai kebutuhan) -->
        🛒
      </a>

      <span>Halo, {{ auth.user.fullName }}</span>

      <form method="POST" action="{{ route('auth.logout') }}">
        {{ csrfField() }}
        <button type="submit" class="bg-red-500 hover:bg-red-600 px-4 py-2 rounded text-sm">Logout</button>
      </form>
    </div>
  @else
    <div class="flex gap-4">
      <a href="{{ route('auth.register.show') }}" class="bg-white text-blue-600 px-4 py-2 rounded hover:bg-blue-100 text-sm">Register</a>
      <a href="{{ route('auth.login.show') }}" class="bg-white text-blue-600 px-4 py-2 rounded hover:bg-blue-100 text-sm">Login</a>
    </div>
  @endif
</header>


  <main class="p-6">
    <h2 class="text-2xl font-semibold mb-4">Pilih Voucher Kamu</h2>


    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
  <!-- Voucher Pulsa -->
  <div class="bg-white rounded-xl shadow p-4 hover:shadow-md transition">
    <h3 class="text-lg font-bold text-blue-600">Voucher Pulsa</h3>
    <p class="text-sm text-gray-600">Dapatkan pulsa murah dari semua operator.</p>
    <a href="{{ route('voucher_pulsa.list') }}">
      <button class="mt-3 bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Lihat Voucher</button>
    </a>
  </div>

  <!-- Voucher Data -->
  <div class="bg-white rounded-xl shadow p-4 hover:shadow-md transition">
    <h3 class="text-lg font-bold text-green-600">Voucher Data</h3>
    <p class="text-sm text-gray-600">Paket data internet dengan harga terbaik.</p>
    <a href="{{ route('voucher_data.list') }}">
      <button class="mt-3 bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700">Lihat Voucher</button>
    </a>
  </div>

  <!-- Voucher Token Listrik -->
  <div class="bg-white rounded-xl shadow p-4 hover:shadow-md transition">
    <h3 class="text-lg font-bold text-yellow-600">Voucher Token Listrik Prabayar</h3>
    <p class="text-sm text-gray-600">Beli token listrik dengan mudah dan cepat.</p>
    <a href="{{ route('voucher_listrik.list') }}">
      <button class="mt-3 bg-yellow-600 text-white px-4 py-2 rounded hover:bg-yellow-700">Lihat Voucher</button>
    </a>
  </div>

  <!-- Voucher Diskon -->
  <div class="bg-white rounded-xl shadow p-4 hover:shadow-md transition">
    <h3 class="text-lg font-bold text-red-600">Voucher Diskon Belanja</h3>
    <p class="text-sm text-gray-600">Voucher diskon untuk berbagai merchant.</p>
    <a href="{{ route('voucher_diskon.list') }}">
      <button class="mt-3 bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700">Lihat Voucher</button>
    </a>
  </div>
</div>




  <footer class="mt-12 text-center text-sm text-gray-500">
    &copy; 2025 Voucherku. By Safik, Vaizal, FathatunNida, Dimas.
  </footer>

</body>
</html>
